import styled from 'styled-components';

export const StyledPeedCardContainer = styled.div`
  display: flex;
  flex-direction: column;
  gap: 32px;

  max-width: 684px;
  padding: 32px;

  box-shadow: var(--shadow1pt);
  border-radius: 16px;
`;
